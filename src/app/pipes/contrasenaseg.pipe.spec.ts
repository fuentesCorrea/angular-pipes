import { ContrasenasegPipe } from './contrasenaseg.pipe';

describe('ContrasenasegPipe', () => {
  it('create an instance', () => {
    const pipe = new ContrasenasegPipe();
    expect(pipe).toBeTruthy();
  });
});
